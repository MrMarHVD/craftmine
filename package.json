{
  "name": "browser-voxel-world",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "mp:dev": "wrangler dev cloudflare/worker.js --config cloudflare/wrangler.toml",
    "mp:deploy": "wrangler deploy cloudflare/worker.js --config cloudflare/wrangler.toml"
  },
  "dependencies": {
    "three": "^0.176.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
